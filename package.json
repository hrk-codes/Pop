{
  "name": "pop",
  "version": "0.1.0",
  "private": true,
  "description": "Permission-aware, context-sensitive Windows desktop AI companion.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present build",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm check:native",
    "check:native": "pnpm --filter @pop/desktop build && cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --check && cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "pnpm --recursive --if-present test",
    "typecheck": "pnpm --recursive --if-present typecheck"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.9.0",
    "globals": "17.11.0",
    "prettier": "3.9.6",
    "typescript": "5.9.2",
    "typescript-eslint": "8.67.0"
  }
}
