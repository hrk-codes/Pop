{
  "name": "@pop/security",
  "version": "0.1.0",
  "private": true,
  "description": "Sensitive-data checks and safe structured logging.",
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json"
  }
}
