{
  "name": "@pop/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Permission-scoped Chrome Manifest V3 context adapter.",
  "type": "module",
  "scripts": {
    "build": "wxt build",
    "dev": "wxt",
    "postinstall": "wxt prepare",
    "test": "vitest run",
    "typecheck": "wxt prepare && tsc --project tsconfig.json"
  },
  "dependencies": {
    "@pop/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "0.1.38",
    "wxt": "0.20.13"
  }
}
