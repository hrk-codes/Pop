{
  "name": "@pop/event-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic suggestions and intent classification.",
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@pop/context": "workspace:*"
  }
}
