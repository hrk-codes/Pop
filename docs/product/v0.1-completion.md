# V0.1 Completion Gate

**Gate result:** BLOCKED

**Audit date:** 2026-08-23

The repository is a verified Phase 0 foundation, not a completed V0.1 product. V0.2 assumes a working
desktop runtime and integrations that do not yet exist. Building Phase 14 on top of placeholders would
hide missing security boundaries and make later behavior impossible to verify.

## Evidence collected

The audit inspected all files under `apps/` and `packages/`, searched non-documentation source for V0.1
runtime behavior, ran the development preflight, and executed `pnpm check` and `pnpm build`.

```text
Formatting:              PASS
ESLint:                  PASS
Strict TypeScript:       PASS
Foundation unit test:    PASS (1 test)
Workspace build command: PASS, but no application has a build script
GitHub CI on main:       PASS
```

Node.js, pnpm, and Git are available. Rust, Cargo, rustup, and Visual Studio C++ Build Tools are not
currently detected, so the planned Tauri application cannot yet be compiled.

## V0.1 capability audit

| Required capability                     | Status  | Repository evidence                                        |
| --------------------------------------- | ------- | ---------------------------------------------------------- |
| Tauri desktop shell                     | Missing | `apps/desktop` contains only a package manifest and README |
| React/TypeScript UI                     | Missing | No desktop `src/`, React dependency, or Vite configuration |
| Draggable, always-on-top companion      | Missing | No window or Tauri configuration                           |
| Compact/expanded modes and tray         | Missing | No UI or native tray implementation                        |
| Monitoring ON/OFF                       | Missing | No monitoring state or core command                        |
| Application allow-list                  | Missing | `packages/permissions` is a documented placeholder         |
| Browser domain allow-list               | Missing | No policy implementation or tests                          |
| Deny-by-default enforcement             | Missing | Principle is documented, but no executable gate exists     |
| Windows foreground detection            | Missing | No Rust/native source exists                               |
| Authenticated localhost communication   | Missing | `packages/protocol` is a documented placeholder            |
| Shared validated message protocol       | Missing | No schemas, server, handshake, or message tests            |
| VS Code extension connection/context    | Missing | No extension source or VS Code dependency                  |
| Chrome extension connection/context     | Missing | No manifest, extension source, or Chrome permissions       |
| AI provider abstraction                 | Missing | `packages/ai` is a documented placeholder                  |
| Groq provider and normalized responses  | Missing | No HTTP/provider source or provider tests                  |
| Minimum-context and sensitive filtering | Missing | Context/security packages contain no implementation        |
| Cloud activity indicator                | Missing | No desktop UI exists                                       |
| What POP Can See panel                  | Missing | No desktop UI exists                                       |

No item above is marked partial merely because a future boundary has a directory. A capability counts
only when executable behavior and proportionate verification exist.

## Requested event traces

### VS Code selection to AI response

There is no existing event to trace. The VS Code adapter, protocol client/server, POP Core, permission
pipeline, event engine, AI provider, and response UI are all absent.

The intended V0.1 trace remains:

```text
VS Code stable selection
  -> adapter collects selection + filename + language
  -> extension validates and sends a versioned local message
  -> POP Core authenticates and validates the client/message
  -> monitoring + app + context permission gates
  -> sensitive-content filter and minimum-context normalization
  -> local event rule proposes Explain
  -> user chooses Explain
  -> visible cloud activity begins
  -> provider-neutral request -> Groq adapter
  -> normalized, validated response
  -> desktop response UI
```

This is a design trace, not a verified runtime trace.

### Chrome selected text to POP

There is no existing event to trace. The Chrome manifest, content script, domain gate, authenticated
transport, protocol handler, and desktop context state are absent.

The intended V0.1 trace remains:

```text
selection changes on an approved domain
  -> Chrome adapter checks domain before collection/forwarding
  -> content is minimized to domain + title + selected text metadata
  -> extension validates and sends a versioned local message
  -> POP Core authenticates and validates the client/message
  -> monitoring + app + domain + context permission gates
  -> sensitive-content filter and normalization
  -> POP shows approved browser context
```

This is also a design trace, not a verified runtime trace.

## Blocking technical debt

1. Install and verify the Windows native toolchain required by Tauri.
2. Complete V0.1 Phases 1-3: desktop shell, persistent local configuration, and foreground awareness.
3. Complete Phases 4-6: authenticated protocol, VS Code adapter, and executable permission engine.
4. Complete Phases 7-8: provider abstraction, Groq adapter, and explicit code-explanation flow.
5. Complete Phases 9-12: Chrome adapter, browser assistance, restrained V0.1 events, and security UI.
6. Add unit, security, protocol, integration, and manual Windows verification for those behaviors.

## Gate decision

Do not start Phase 14 implementation yet. Resume the unfinished V0.1 sequence at Phase 1 and advance
only after each phase is demonstrated. This preserves the master prompt's requirement to repair missing
V0.1 foundations before adding dependent V0.2 functionality.
